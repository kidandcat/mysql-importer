{
  "name": "sql-importer",
  "version": "1.0.0",
  "description": "Module for import a sql script to MySQL if a given database does not exists",
  "main": "importer.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jairo Caro-Accino Viciana kidandcat@gmail.com",
  "license": "MIT",
  "dependencies": {
    "mysql": "^2.10.2"
  }
}
